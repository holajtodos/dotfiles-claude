# <Project name>

The ubiquitous language for <what the project builds>. When code, an issue
title, or a commit message names one of these concepts, it uses the term as
defined here. Decisions with a wider blast radius live in `docs/adr/`.

## Language

### <Group of terms>

**`<Term>`**:
<Definition in one to three sentences: what it is, what it is not, and the
boundary that matters.>
_Avoid_: <synonyms the project does not use>

**<Term>** — *<expansion if an acronym>*:
<Definition.>
_Avoid_: <synonyms>

## Glossary maintenance

A term enters this file when a grilling or an ADR resolves it, not before.
Each entry names what to avoid so the vocabulary does not drift.
