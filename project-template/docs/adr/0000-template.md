# <N>. <Decision as a noun phrase>

## Status

Proposed (<YYYY-MM-DD>). <Becomes "Accepted (date)"; amendments are appended
as dated sections at the end, never edited into the original text.>

## Context

<The forces: constraints of the host, the prior code's choice, the
alternatives actually on the table, with numbers where they decided it.>

## Decision

<What was chosen, stated so a reader can check code against it: names, env
vars, defaults, invariants.>

## Consequences

<What becomes easier, what becomes harder, what the pipeline must now do
itself, and what a later ticket is expected to revisit.>
